'use client';

import { Section, FormHeading } from '@/shared/components';

export default function MyCart() {
  return (
    <Section className="py-[80px] pt-[43px]">
      <FormHeading heading="Оформлення замовлення" />
    </Section>
  );
}
